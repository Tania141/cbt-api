"""
rules — правилата за верификация на строителното досие.

ЕДИН ИЗТОЧНИК. Всяко правило е функция, която носи в себе си:
  · цитата от закона, на който стъпва
  · какво проверява, на човешки език
  · реалните случаи, с които се доказва

От тях се произвежда четимият документ (`tools/rules_doc.py`) — както
`dump_templates.py` прави .md огледалата на шаблоните. Така описанието и
правилото не могат да се разминат: описанието се генерира от правилото.

Един и същ труд върши три работи:
  1. работеща проверка в системата днес
  2. техническо задание за външен изпълнител
  3. приемен критерий при предаването

ТРИ ПРИСЪДИ, не две:
  OK        — проверено, минава
  WARN      — проверено, не минава
  UNKNOWN   — липсват данни, правилото изчаква

Третата е задължителна. Без нея всеки нормален строеж свети червено, защото
половината дати още ги няма — и операторът спира да гледа предупрежденията.
"""
from .engine import Rule, Verdict, OK, WARN, UNKNOWN, run_all, ALL_RULES
from . import dates     # noqa: F401  — правилата за срокове
from . import required  # noqa: F401  — изискваният набор по категория
from . import team      # noqa: F401  — екипът на обекта: има ли кой да подпише
from . import kategoria # noqa: F401  — категорията срещу цитираните разпоредби
from . import gradoustroystvo  # noqa: F401  — проект ≤ виза ≤ Наредба № 7

__all__ = ["Rule", "Verdict", "OK", "WARN", "UNKNOWN", "run_all", "ALL_RULES"]
